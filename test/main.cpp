#include <iostream>
#include <gtest/gtest.h>

using namespace std;

TEST(MultidimensionalList, create_number) {
    ASSERT_TRUE(5 == 5);
}

TEST(MultidimensionalList, create_list) {
    
    ASSERT_TRUE(true);
}
